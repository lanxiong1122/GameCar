<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.charon.racegame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/car_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Racegame">
        <activity android:name=".activity.CarActivity">

        </activity>
        <activity android:name=".activity.RankPageActivity"></activity>
        <activity android:name=".activity.NavigationPageActivity" />
        <activity android:name=".activity.AdvertisingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>